---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Name }}
spec:
  accessModes:
    - ReadOnlyMany
  capacity:
    storage: "16Ti"
  nfs:
    path: {{ .Values.path }}
    server: {{ .Values.server }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}
spec:
  volumeName: {{ .Release.Name }}
  storageClassName: ""
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: {{ .Values.size }}
